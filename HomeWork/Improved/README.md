<div align="center">

# HomeWork - Impoved versions

>[!tip]
Сейчас вы находитесь в: 
`pythondeveloping` > `Homework` > `Improved` > `README.md`


## Добро пожаловать в папку с улучшенными версиями моих домашних заданий.
</div>


>[!note]
> ### Назначение этой папки
> Здесь собраны **переработанные и оптимизированные** версии моих учебных проектов.
> каждый файл содержит исправление ошибок, улучшенную структуру и соблюдение стандартам **PEP8**

>[!tip]
> ### Цели улучшенных версий:
> **Исправить критические ошибки** с прошлых версий  
> **Привести код в соотвествие к стандартам PEP8**   
> **Добавить обработку исключений и валидацию данных**  
> **Улучшить структуру и читаемость кода**   
> **Добавить документацию и комментарии**  
> **Оптимизировать производительность**


>[!important]
> ### Важное замечание
> Все улучшенные версии **сохраняют оригинальный функционал**, но делают его более надежным, безопасным и профессиональным.


# *Глава 1.* *Осень*

# Lesson_1-improved.py


```py
import datetime


current_year = datetime.datetime.now().year


name = input("Введите ваше имя: ")


while True:
    try:
        birth_year = int(input("Введите ваш год рождения: "))
        
        if 1900 <= birth_year <= current_year:
            break
        else:
            print(f"пожалуйста, введите год между 1900 и {current_year}")
    except ValueError:
        print("ошибка! введите целое число для года рождения")


hobby = input("Введите ваше хобби: ")


age = current_year - birth_year

print("\n" + "*" * 40)
print(f"добрый день, {name}")
print(f"вам: {age} лет.")
print(f"ваше любимое хобби: {hobby}")
print("*" * 40)

print("\n" + "-" * 40)
print("дополнительная информация:")
print(f"текущий год: {current_year}")
print(f"вы родились в {birth_year} году")

# проверка на совершеннолетие
if age >= 18:
    print("вы совершеннолетний, Вы получите повестку на портале Госуслуги.")
else:
    print("вы несовершеннолетний")
```

## ✅ Что было добавлено:
1. Необходимая библиотека `datetime`
2. Валидация ввода года рождения
```py
while True:
    try:
        birth_year = int(input("Введите ваш год рождения: "))
        if 1900 <= birth_year <= current_year:
            break
        else:
            print(f"пожалуйста, введите год между 1900 и {current_year}")
    except ValueError:
        print("ошибка! введите целое число для года рождения")
```
3. Дополнительная аналитика и вывод
```py
if age >= 18:
    print("вы совершеннолетний")
else:
    print("вы несовершеннолетний")
```
4. подробные комментарии
```py
# проверка на совершеннолетие
```
5. улучшенное форматирование вывода
```py
print("\n" + "-" * 40)
print("дополнительная информация:")
```


## ❌ Что было убрано:
1. Фиксированный текущий год
```py
# Было
current_year = 2025

# Стало
current_year = datetime.datetime.now().year
```
2. Отсутствие проверки ввода
```py
# Было (без проверки)
birth_year = int(input("Введите ваш год рождения: "))

# Стало (с проверкой)
while True:
    try:
        birth_year = int(input("Введите ваш год рождения: "))
```

# Lesson_2-improved.py

```py
import time
import random

# Простая текстовая игра про поиск сокровищ

print("=" * 50)
print("🏴  ПРИКЛЮЧЕНИЕ НА КОРАБЛЕ")
print("=" * 50)

# Получаем имя игрока
name = input("Как вас зовут, капитан? ")
print(f"\nОтлично, капитан {name}! Начинаем плавание!")

if name.lower() == "максим":
    print("Опа, Максим мурино")


time.sleep(1)
print("\n" + "-" * 50)
print("ИСТОРИЯ:")
print(f"Вы, капитан {name}, плывете на своем корабле в поисках")
print("легендарного сокровища. Но внезапный Максим сбил вас с курса!")
print("Теперь вы должны решить, куда плыть дальше.")
print("-" * 50)

time.sleep(2)
print("\n ПРАВИЛА ИГРЫ:")
print("• У вас есть 3 попытки")
print("• Выберите одно из направлений:")
print("  север, юг, запад, восток")
print("• Каждое направление ведет к разному результату!")
print()


attempts = 3  # Количество попыток
score = 0     # Счет игрока

while attempts > 0:
    print(f"\n  Осталось попыток: {attempts}")
    print(f" Ваш счет: {score}")
    
    
    while True:
        print("\nКуда плывем, капитан?")
        print("(север/юг/запад/восток)")
        choice = input("> ").lower().strip()
        
        
        if choice in ["север", "юг", "запад", "восток", "с", "ю", "з", "в"]:
            break
        else:
            print("Ты что макан, я не шарю что ты написал там, попробуй ещё раз.")
    
    
    if choice == "с": choice = "север"
    elif choice == "ю": choice = "юг"
    elif choice == "з": choice = "запад"
    elif choice == "в": choice = "восток"
    
    # добавляем случайное событие (чтобы было интереснее)
    events = [
        "встретили стаю дельфинов",
        "попали в легкий шторм",
        "увидели летающих рыб",
        "нашли старую карту в бутылке",
        "встретили торговое судно"
        "встретили Максима который не сделал ДЗ"
    ]
    random_event = random.choice(events)
    
    print(f"\n  Вы плывете на {choice}...")
    time.sleep(1)
    print(f" По пути вы {random_event}!")
    time.sleep(1)
    
    # проверяем результат
    print("\n" + "~" * 50)
    
    if choice == "север":
        print("  Вы доплыли до северных фогов!")
        print(" Нашли сундук с золотом викингов!")
        print(" +100 очков!")
        score += 100
        attempts -= 1
        
    elif choice == "юг":
        print("  Вы достигли тропических островов!")
        print(" Но там были пираты... Они забрали все!")
        print(" Потеряна 1 попытка")
        attempts -= 1
        
    elif choice == "запад":
        print(" Вы плыли на закат")
        print(" Ничего не нашли, возвращаемся назад")
        print(" Попытка не потрачена!")
        
        
    elif choice == "восток":
        print("  Вы достигли острова сокровищ!")
        print(" НАШЛИ ГЛАВНОЕ СОКРОВИЩЕ!")
        print(" +200 очков!")
        score += 200
        attempts -= 1
    
    print("~" * 50)
    time.sleep(2)
    
    
    if attempts > 0:
        print("\nПродолжаем поиски?")
        print("(да/нет)")
        continue_choice = input("> ").lower().strip()
        
        if continue_choice in ["нет", "н", "стоп", "хватит"]:
            print("\n Лан")
            break


print("\n" + "=" * 50)
print(" ИГРА ЗАКОНЧЕНА!")
print("=" * 50)
print(f" Игрок: {name}")
print(f" Финальный счет: {score}")
print(f" Попыток использовано: {3 - attempts}")

# 6. определяем уровень успеха
if score >= 200:
    print(" Звание: ЛЕГЕНДАРНЫЙ КАПИТАН")
elif score >= 100:
    print(" Звание: ОПЫТНЫЙ МОРЕПЛАВАТЕЛЬ")
else:
    print(" Звание: НАЧИНАЮЩИЙ МОРЯК")

print("\nСпасибо за игру! ")
print("=" * 50)
```

## ✅ Что было добавлено:
1. Игра теперь не ломается.
Можно писать `СЕВЕР`, `Cевер`, `север` или даже `c`
2. Система очков и попыток
3. Сделал игру более интереснее
4. Красивый вывод

## ❌ Что было убрано:
1. Лишняя библиотека `winsound`  
```py
# Было
import winsound
winsound.Beep(1000,500)

# Стало (убрано совсем)
# - Звук удален, так как он работал только на Виндовс
# - Теперь игра будет работать на любом компьютере
```
2. Жесткие требования к вводу
```py
# Было
print("Уважаемый {name}, пожалуйста пишите направление с маленькой буквы.")
# Игра ломалась если: "ЮГ", "юг ", "СЕВЕР" и т.д.

# Стало (любой ввод)
choice = input("> ").lower().strip()
# Принимает: "ЮГ", "юг", "юг ", "Юг" → всё становится "юг"
```
3. Гневные сообщения о ошибках
```py
# Было (кричит на игрока)
print("ААА ВВЕДИ УЖЕ ПРАВИЛЬНО НАПРАВЛЕНИЕ С СТРОЧНОЙ БУКОВКИ!!!!")

# Стало (вежливо объясняет)
print("Ты что макан, я не шарю что ты написал там, попробуй ещё раз.")
```
4. Одноразовые концовки
```py
# Было (всегда одинаково)
if napravlenie == "юг":
    print("Вы проиграли! Вас убил злой циклоп!")

# Стало (с случайными событиями)
events = ["встретили стаю дельфинов", "попали в легкий шторм", "Вы встретили Максима"...]
random_event = random.choice(events)
print(f"По пути вы {random_event}")
```
5. Ограничение только 4 словами
```py
# Было (только полные слова)
if napravlenie == "юг":  # Только "юг"

# Стало (и сокращения работают)
if choice == "с": choice = "север"
elif choice == "ю": choice = "юг"
# Принимает: "с", "ю", "з", "в" - сокращения направлений
```
6. Конец игры при ошибке
```py
# Было (одна ошибка = конец):
else:
    print("ААА ВВЕДИ УЖЕ ПРАВИЛЬНО...")
    # Игра заканчивается

# Стало (можно пробовать снова):
while True:
    choice = input("> ").lower().strip()
    if choice in ["север", "юг", "запад", "восток", "с", "ю", "з", "в"]:
        break  # Правильный ввод - выходим из цикла
    else:
        print("Ты что макан, я не шарю что ты написал там, попробуй ещё раз")  # Просим ввести снова
        # Игра НЕ заканчивается
```
7. Фиксированные описания
```py
# Было (всегда одно и то же)
elif napravlenie == "восток":
    print("Вы приплыли к сокровищу! Вы нашли 200 настоящих рублей!!!")

# Стало (более интересные описания)
elif choice == "восток":
    print("Вы достигли острова сокровищ")
    print("НАШЛИ ГЛАВНОЕ СОКРОВИЩЕ")
    print("+200 очков!")
```

~~# Lesson_3-improved.py~~
>[!warning]
> ### **Lesson_3 не существует.**

# Lesson_4-improved.py

```py
# часть 1: Сумма чисел от 1 до n

while True:
    try:
        n = int(input("Введите положительное число: "))
        if n > 0:
            break
        else:
            print("Число должно быть больше 0!")
    except:
        print("Ошибка! Введите целое число.")

sum_result = 0

for number in range(1, n + 1):
    sum_result += number

print(f"Сумма чисел от 1 до {n} = {sum_result}")

print("=" * 50)

# часть 2: четные и нечетные числа

print("\nЧетные числа от 1 до 20:")

counter = 1

while counter <= 20:
    if counter % 2 == 0:
        print(counter)
    counter += 1

print("\nНечетные числа от 1 до 20:")

counter = 1

while counter <= 20:
    if counter % 2 != 0:
        print(counter)
    counter += 1

print("=" * 50)

# часть 3: дополнительно - красивое отображение

print("\nвсе числа от 1 до 20:")

for i in range(1, 21):
    if i % 2 == 0:
        print(f"{i} (четное)")
    else:
        print(f"{i} (нечетное)")

print("\n" + "=" * 50)
print("Максим Мурино успешно завершил этот код")
print("=" * 50)
```
## ✅ Что было добавлено:
1. Проверка ввода (обработка ошибок)
```py
while True:
    try:
        n = int(input("Введите положительное число: "))
        if n > 0:
            break
        else:
            print("Число должно быть больше 0")
    except:
        print("Ошибка! Максим мурино хочет целое число")
```
2. Красивый вывод
```py
print("=" * 50)
print("=" * 50)
```
3.Сброк счетчика для второго цикла
```py
# Для четных чисел
counter = 1  # Начинаем с 1
while counter <= 20:

# Для нечетных чисел
counter = 1  # СНОВА начинаем с 1
while counter <= 20:
```
4. Доп. часть (красивое оформление)
```py
for i in range(1, 21):
    if i % 2 == 0:
        print(f"{i} (четное)")
    else:
        print(f"{i} (нечетное)")
```

## ❌ Что было убрано:
1. Ошибка с переменной `i`
```py
# Было (ошибка - i не определена)
while i <= 20:  # MAKSIM ERROR! i уже использовалась в for цикле

# Стало (новый счетчик)
counter = 1  # Всегда начинаем с начала
while counter <= 20:
```
2. Неправильный вывод в циклах
```py
# Было (печатает chislo вместо i)
if i % 2 == 0:
    print(chislo) # MAKSIM ERROR! печатает всегда 1

# Стало (печатает правильное число)
if counter % 2 == 0:
    print(counter)  # Правильно печатает счетчик
```
3. Отсутствие обработки ошибок
```py
# Было (если ввести букву - программа сломается):
chislo1 = int(input("Введи число "))  # MAKSIM ERROR при вводе "abc"

# Стало (защита от ошибок) 
try:
    n = int(input("Введите положительное число: "))
except:
    print("Максим мурино требует целое число.")
```
4. Отсутствие проверки на полож. числа
```py
# Было (можно ввести 0 или отрицательное)
chislo1 = int(input("Введи число "))  # Можно ввести -1488

# Стало (только положительные)
if n > 0:
    break
else:
    print("Число должно быть больше 0")
```

# Lesson_5-improved.py

```py
numbers = []

for count in range(10):
    while True:
        try:
            number = int(input(f"Введите целое число #{count + 1}: "))
            numbers.append(number)
            break
        except ValueError:
            print("Ошибка Целое число введи ало")

print(f"\nСортированный список: {sorted(numbers)}")
print("*" * 40)

text = "the quick brown fox jumps over the lazy dog."

lower_text = text.lower()
text_without_dots = lower_text.replace(".", "")
words = text_without_dots.split()
long_words = [word for word in words if len(word) > 3]

longest_word = ""
for word in long_words:
    if len(word) > len(longest_word):
        longest_word = word

print(f"Текст в нижнем регистре: {lower_text}")
print(f"Текст без точки: {text_without_dots}")
print(f"Список слов: {words}")
print(f"Слова длиннее 3 символов: {long_words}")

if longest_word:
    print(f"Самое длинное слово: '{longest_word}' (длина: {len(longest_word)})")
else:
    print("Нет слов длиннее 3 символов")

print("*" * 40)

print("\nДополнительная информация:")
print(f"Всего слов: {len(words)}")
print(f"Длинных слов: {len(long_words)}")

if words:
    average_length = sum(len(word) for word in words) / len(words)
    print(f"Средняя длина слова: {average_length:.1f} символов")

print("\nРаспределение по длине слов:")
for length in range(1, 11):
    words_with_length = [word for word in words if len(word) == length]
    if words_with_length:
        print(f"  {length} символов: {len(words_with_length)} слов")
```

## ✅ Что было добавлено:
1. обработка ошибок ввода чисел
```py
try:
    number = int(input(...))
except ValueError:
    print("Ошибка! Целое число введи ало")
```
2. Защита от пустого списка длинных слов (хз зачем, ну пусть будет)
```py
if longest_word:
    print(f"Самое длинное слово: '{longest_word}'")
else:
    print("Нет слов длиннее 3 символов")
```
3. Доп. статистика
```py
print(f"Всего слов: {len(words)}")
print(f"Длинных слов: {len(long_words)}")
average_length = sum(len(word) for word in words) / len(words)
```
4. Распределение слов по длине
```py
for length in range(1, 11):
    words_with_length = [word for word in words if len(word) == length]
```
5. Нумерация ввода чисел
```py
input(f"Введите целое число #{count + 1}: ")
```

## ❌ Что было убрано: 
*ничего*  
Я буквально не нашёл то, что можно было убрать.


# Lesson_6.-improved.py

## ✅ Что было добавлено:
*ничего*
Улучшенний не обнаружено.

## ❌ Что было убрано:
*ничего*
Я буквально не нашёл то, что можно было убрать.

# Lesson_7-improved.py

```py

def rectangle_info(length, width):

    perimeter = 2 * (length + width)
    area = length * width
    diagonal = (length**2 + width**2)**0.5
    
    print("\n" + "="*50)
    print(" ПАРАМЕТРЫ ПРЯМОУГОЛЬНИКА")
    print("="*50)
    print(f" Длина: {length}")
    print(f" Ширина: {width}")
    print(f" Периметр: {perimeter:.2f}")
    print(f" Площадь: {area:.2f}")
    print(f" Диагональ: {diagonal:.2f}")
    
    if length == width:
        print(" Это квадрат Максима!")
    print("="*50)
    
    return perimeter, area, diagonal


def fibonacci_sequence(n):

    if n <= 0:
        print("Ошибка: n должно быть положительным числом!")
        return []
    
    sequence = []
    
    if n >= 1:
        sequence.append(0)
    if n >= 2:
        sequence.append(1)
    
    for i in range(2, n):
        next_number = sequence[i-1] + sequence[i-2]
        sequence.append(next_number)
    
    return sequence


def print_fibonacci_info(sequence, n):
    
    if not sequence:
        return
    
    print("\n" + "="*50)
    print("🌀 ПОСЛЕДОВАТЕЛЬНОСТЬ ФИБОНАЧЧИ")
    print("="*50)
    
    print(f"🔢 Первые {n} чисел:")
    
   
    for i in range(0, len(sequence), 10):
        chunk = sequence[i:i+10]
        print("   " + " ".join(f"{num:>4}" for num in chunk))
    
    print(f"\n📈 Статистика:")
    print(f"   Количество чисел: {len(sequence)}")
    print(f"   Сумма: {sum(sequence)}")
    
    if len(sequence) >= 2:
        last_ratio = sequence[-1] / sequence[-2]
        print(f"   Отношение последних: {last_ratio:.6f}")
    
    
    max_number = max(sequence) if sequence else 0
    print(f"   Максимальное число: {max_number}")
    
    print("="*50)


def golden_ratio_approximation(n):
    
    if n < 3:
        return None
    
    sequence = fibonacci_sequence(n)
    if len(sequence) < 2 or sequence[-2] == 0:
        return None
    
    return sequence[-1] / sequence[-2]



def main():
    print("="*60)
    print("🧮 МАТЕМАТИЧЕСКИЕ РАСЧЕТЫ")
    print("="*60)
    
    # Часть 1: Прямоугольник
    print("\n" + ""*20)
    print("ЧАСТЬ 1: ПРЯМОУГОЛЬНИК")
    print("📐"*20)
    
    print("\n Пример 1: прямоугольник")
    rectangle_info(5, 8)
    
    print("\n пример 2: квадрат 7×7")
    rectangle_info(7, 7)
    
    # часть 2: числа Фибоначчи
    print("\n" + ""*20)
    print("ЧАСТЬ 2: ЧИСЛА ФИБОНАЧЧИ")
    print(""*20)
    
    # Несколько примеров
    test_numbers = [10, 15, 20]
    
    for n in test_numbers:
        sequence = fibonacci_sequence(n)
        print_fibonacci_info(sequence, n)
        
        if len(sequence) >= 3:
            golden_approx = golden_ratio_approximation(n)
            true_golden = 1.618033988749895
            if golden_approx:
                print(f"   Приближение φ: {golden_approx:.8f}")
                print(f"   Истинное φ:    {true_golden:.8f}")
                error = abs(true_golden - golden_approx)
                print(f"   Погрешность:   {error:.8f}")
                print()
    
    # Часть 3: п ользовательский ввод
    print("\n" + ""*20)
    print("ЧАСТЬ 3: ВАШИ РАСЧЕТЫ")
    print(""*20)
    
    try:
        user_length = float(input("\nВведите длину прямоугольника: "))
        user_width = float(input("Введите ширину прямоугольника: "))
        rectangle_info(user_length, user_width)
        
        user_n = int(input("\nСколько чисел Фибоначчи вывести? "))
        user_sequence = fibonacci_sequence(user_n)
        print_fibonacci_info(user_sequence, user_n)
        
    except ValueError as oshibka:
        print(f"Ошибка ввода.Ошибка: {oshibka}")

main()
```

## ✅ Что было добавлено:
1. Проверка на квадрат
```py
if length == width:
    print(" Это квадрат Максима!")
```
2. Красивый формат ввода
```py
print(" ПАРАМЕТРЫ ПРЯМОУГОЛЬНИКА")
print(f" Длина: {length}")
print(f" Ширина: {width}")
```
3. Вывод чисел финобаччи таблицей
```py
for i in range(0, len(sequence), 10):
    chunk = sequence[i:i+10]
    print("   " + " ".join(f"{num:>4}" for num in chunk))
```
4. Статистика
```py
print(f"Количество чисел: {len(sequence)}")
print(f"Сумма: {sum(sequence)}")
print(f"Максимальное число: {max_number}")
```
5. Ввод пользователя через `float(input(""))`
```py
user_length = float(input("\nВведите длину прямоугольника: "))
user_sequence = fibonacci_sequence(user_n)
```
6. Обработка ошибок
```py
except ValueError:
    print("Ошибка")
```


## ❌Что было убрано:
1. Переменные на кириллице (соблюдаем стандарт **PEP8**)
2. Напоминание Максиму делать дз (зря убрал)
3. капс лок и восклицательные знаки
4. сложные имена переменных (`ДРУГОЕ_ФИБОНАЧЧИ_МАКСИМА_МУРИНО`),
(`ЭТОТ_ЖАЛКИЙ_ПЕРИМЕТР_МАКСИМА_МУРИНО`)

# Lesson_8-improved.py

```py
import tkinter as tk
from tkinter import messagebox

def calculator():
    try:
        a = float(entry1.get())
        b = float(entry2.get())
        
        # Выполняем все операции
        results = []
        results.append(f"Сложение: {a} + {b} = {a + b}")
        results.append(f"Вычитание: {a} - {b} = {a - b}")
        results.append(f"Умножение: {a} × {b} = {a * b}")
        
        if b != 0:
            results.append(f"Деление: {a} ÷ {b} = {a / b:.2f}")
        else:
            results.append("Деление: НА 0 ДЕЛИТЬ НЕЛЬЗЯ!")
        
        result_label.config(text="\n".join(results))
        
    except ValueError:
        messagebox.showerror("Ошибка!", "Введите числа!")

# Создаем окно
window = tk.Tk()
window.title("Калькулятор")
window.geometry("300x300")

# Поля ввода
tk.Label(window, text="Первое число:").pack(pady=5)
entry1 = tk.Entry(window, width=30)
entry1.pack(pady=5)

tk.Label(window, text="Второе число:").pack(pady=5)
entry2 = tk.Entry(window, width=30)
entry2.pack(pady=5)

# Кнопка расчета
tk.Button(window, text="Вычислить", command=calculator, bg="lightblue", width=20).pack(pady=10)

# Поле для результата
result_label = tk.Label(window, text="Результат:", justify="left")
result_label.pack(pady=10)

# Запуск приложения
window.mainloop()
```

## ✅ Что было ~~добавлено~~ оставлено:
1. ввод первого числа
2. ввод второго числа
3. результат 
4. кнопка "вычислить"

## ❌ Что было убрано:
Все допросы про MAX  
Все тюрьмы и наказания  
Случайные проверки  
Выключение компьютера  
Ссылки на сайты  
Кнопки запуска игр  
Второе окно (MAЙНКР*ВТ)  
"Полезные кнопки"  
Все мемы и шутки  
Сложная логика с выбором операций  


# Lesson_9-improved.py
```py
import tkinter as tk
import random
import time
from tkinter import messagebox

# Основное окно
window = tk.Tk()
window.title("Домашние задания")
window.geometry("400x500")

# ===================== ДЗ 1: ШАРМАНКА =====================
def dz1_sharmanka():
    dz1_window = tk.Toplevel(window)
    dz1_window.title("ДЗ 1 - ШАРМАНКА")
    dz1_window.geometry("300x200")
    
    result_label = tk.Label(dz1_window, text="", font=("Arial", 24))
    result_label.pack(pady=20)
    
    def generate_number():
        number = random.randint(1, 1000)
        result_label.config(text=str(number), fg="green")
    
    tk.Button(dz1_window, text="ЗАПУСТИТЬ ШАРМАНКУ", 
             command=generate_number, bg="green", fg="white").pack(pady=10)

# ===================== ДЗ 2: РАДУЖНАЯ ШАРМАНКА =====================
def dz2_rainbow_sharmanka():
    dz2_window = tk.Toplevel(window)
    dz2_window.title("ДЗ 2 - РАДУЖНАЯ ШАРМАНКА")
    dz2_window.geometry("600x200")
    
    frame = tk.Frame(dz2_window)
    frame.pack(pady=20)
    
    number_labels = []
    for i in range(11):
        label = tk.Label(frame, text="", font=("Arial", 20), width=3)
        label.pack(side=tk.LEFT, padx=5)
        number_labels.append(label)
    
    def generate_rainbow():
        colors = ["red", "orange", "yellow", "green", "blue", 
                 "dark blue", "purple", "pink", "brown", "gray", "cyan"]
        
        for i in range(11):
            number = random.randint(0, 9)
            number_labels[i].config(text=str(number), fg=colors[i])
    
    tk.Button(dz2_window, text="ЗАПУСТИТЬ РАДУЖНУЮ ШАРМАНКУ", 
             command=generate_rainbow, bg="purple", fg="white").pack(pady=10)

# ===================== ДЗ 3: ГЕНЕРАТОР НОМЕРОВ =====================
def dz3_phone_generator():
    dz3_window = tk.Toplevel(window)
    dz3_window.title("ДЗ 3 - ГЕНЕРАТОР НОМЕРОВ")
    dz3_window.geometry("500x250")
    
    phone_label = tk.Label(dz3_window, text="", font=("Arial", 24, "bold"))
    phone_label.pack(pady=30)
    
    def generate_phone():
        region = ''.join([str(random.randint(0, 9)) for _ in range(3)])
        first_part = ''.join([str(random.randint(0, 9)) for _ in range(3)])
        second_part = ''.join([str(random.randint(0, 9)) for _ in range(2)])
        third_part = ''.join([str(random.randint(0, 9)) for _ in range(2)])
        
        phone_number = f"+7 ({region}) {first_part}-{second_part}-{third_part}"
        phone_label.config(text=phone_number, fg="blue")
    
    def copy_phone():
        phone = phone_label.cget("text")
        if phone:
            dz3_window.clipboard_clear()
            dz3_window.clipboard_append(phone)
            messagebox.showinfo("СКОПИРОВАНО", f"Номер {phone} скопирован!")
    
    tk.Button(dz3_window, text="СГЕНЕРИРОВАТЬ НОМЕР", 
             command=generate_phone, bg="blue", fg="white", font=("Arial", 14)).pack(pady=10)
    
    tk.Button(dz3_window, text="КОПИРОВАТЬ НОМЕР", 
             command=copy_phone, bg="green", fg="white").pack(pady=5)

# ===================== ОСНОВНОЕ МЕНЮ =====================
tk.Label(window, text="ДОМАШНИЕ ЗАДАНИЯ", 
        font=("Arial", 20, "bold"), fg="darkblue").pack(pady=20)

tk.Label(window, text="Выберите задание:", 
        font=("Arial", 12)).pack(pady=10)

# Кнопки для ДЗ
tk.Button(window, text="ДЗ 1: ШАРМАНКА", 
         command=dz1_sharmanka, bg="green", fg="white", 
         width=20, height=2, font=("Arial", 12)).pack(pady=10)

tk.Button(window, text="ДЗ 2: РАДУЖНАЯ ШАРМАНКА", 
         command=dz2_rainbow_sharmanka, bg="purple", fg="white", 
         width=20, height=2, font=("Arial", 12)).pack(pady=10)

tk.Button(window, text="ДЗ 3: ГЕНЕРАТОР НОМЕРОВ", 
         command=dz3_phone_generator, bg="blue", fg="white", 
         width=20, height=2, font=("Arial", 12)).pack(pady=10)

# Информация
tk.Label(window, text="\nПростой и понятный интерфейс\nдля выполнения домашних заданий", 
        font=("Arial", 10), fg="gray").pack(pady=20)

# Запуск приложения
window.mainloop()
```

## ✅ Что было ~~добавлено~~ оставлено:
1. Дз1
2. Дз2
3. Дз3

## ❌ Что было убрано:
Все тюрьмы, наказания и 1488    
Все уведомления Windows (notification)     
Все "пасхалки" и шутки про Максима     
Все библиотеки кроме tkinter и random     
Все "секретки" и дополнительные окна     
Все ссылки и браузерные вызовы     
Все выключения компьютера     
Все игры (CS2, майнкрафт, русская рулетка)     
Все квизы и допросы     
Все "полезные кнопки"     
Все анимации загрузки (yaspin)    
Все цветные терминальные сообщения     
Все дополнительные окна и функции     

# *Конец 1 главы - Осень.*

## Общая статистика:
Пройдено уроков: **9**  
Общий объем кода:**~2000+ строк**  
Использовано библиотек: **8+**   
Средняя оценка сложности: **7/10**


##  Технический прогресс
| Уровень | Начало главы | Конец главы | Рост |
|---------|--------------|-------------|------|
| **Сложность кода** | 10 строк | 1488 строк | +14800% |
| **Библиотеки** | 0 | 8+ | +∞ |
| **Типы проектов** | Консольные скрипты | GUI приложения | +300% |
| **Архитектура** | Линейный код | ООП + модульность | +200% |

### 1. Основы Python (Уроки 1,2, ~~3~~ 4)
- ✅ Работа с переменными и типами данных
- ✅ Ввод/вывод данных (`input()`, `print()`)
- ✅ Условные операторы (`if/elif/else`)
- ✅ Циклы (`for`, `while`)
- ✅ Обработка исключений (`try/except`)
- ✅ Работа со строками и списками

### 2. Графика и визуализация (Урок 6)
- ✅ Библиотека `turtle` для рисования
- ✅ Создание сложных графических композиций
- ✅ Работа с координатами и цветами
- ✅ Анимация и интерактивность


### 3. Функции и математика (Уроки 5, 7)
- ✅ Создание пользовательских функций
- ✅ Работа с математическими операциями
- ✅ Последовательность Фибоначчи
- ✅ Геометрические расчеты
- ✅ Рекурсивные алгоритми


### 4. Графические интерфейсы (Уроки 8-9)
- ✅ Библиотека `tkinter` для GUI
- ✅ Создание окон и виджетов
- ✅ Обработка событий
- ✅ Диалоговые окна (`messagebox`)
- ✅ Многомодульные приложения

### 5. Продвинутые технологии
- ✅ Работа с внешними библиотеками
- ✅ Системные уведомления (`plyer`)
- ✅ Терминальная анимация (`yaspin`)
- ✅ Цветной вывод в консоль (`colorama`)
- ✅ Интеграция с браузером (`webbrowser`)


### 1. Учебные проекты (⭐1)
- **Lesson_1:** Ввод данных пользователя
- **Lesson_2:** Текстовая приключенческая игра  
- **Lesson_4:** Работа с числами и циклами
- **Lesson_5:** Анализ текста и списков

### 2. Творческие проекты (⭐3)
- **Lesson_6:** Графический дом с turtle
- **Lesson_7:** Математические функции и Фибоначчи

### 3. Комплексные приложения (⭐5)
- **Lesson_8:** Интерактивный калькулятор с GUI
- **Lesson_9:** Многофункциональное приложение с играми


> *Что будет дальше?*
# **[Главный репозиторий](https://github.com/Drugoi-Polz/IservNG)**

>[!warning]
>### ВНИМАНИЕ
> **Главным репозиторием не владею и *никак* не могу редактировать там, что есть.**


